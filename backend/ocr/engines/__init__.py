"""Multi-engine OCR ensemble for prescription recognition.

Import submodules directly (e.g. ``from backend.ocr.engines.ensemble import
run_ensemble``) to avoid eager loading and circular imports.
"""
